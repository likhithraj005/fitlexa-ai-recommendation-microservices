spring.application.name=eureka

#8761 is the default & standard Eureka dashboard port.
server.port=8761


#Eureka server should not behave like a client
#This app IS the registry, It should NOT register itself
eureka.client.register-with-eureka=false

#It doesn’t need to discover other services, It only stores registry info
eureka.client.fetch-registry=false